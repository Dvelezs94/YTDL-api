"""
This is a pydantic model for Users, which means this is just used for formatting data coming from HTTP payload
Unlike SCIMCore this formats incoming data so we can work with it
This acts as a sanitizer for payloads
"""

import random
import string


class Video():
    def __init__(self):
        return

    def download(src: string):
        return
    
    def transform_to_mp3():
        return
    
    def upload_to_storage():
        return
